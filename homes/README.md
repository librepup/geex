# Homes Configuration Files
## Installation
If you do not want to use the default `home.scm`, simply move the `home.scm` in the root of the `Geex` directory into this directory, and move any `*-home.scm` out.
